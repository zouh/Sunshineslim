<% provide(:title, '注册') %>
<h1>注册</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "新建帐户", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
